spring:
  datasource:
    url: jdbc:postgresql://db.mitikgxwmklcbzqhozgh.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: Bulet@95027
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # use validate in prod
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
